import { div } from 'framer-motion/client'
import React, { useState } from 'react'

const MercadoPagoWallet = () => {
    const[preferencedId, setPreferencedId] = useState(null)
    const publicKey = ""
    const createPreferencedIdEndpoint = ""
    
    return(
        <div> Realizar pago</div>
    )
}

export default MercadoPagoWallet;
